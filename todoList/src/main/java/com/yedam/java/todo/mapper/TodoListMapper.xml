<?xml version="1.0" encoding="UTF-8"?>\
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.java.todo.mapper.TodoListMapper">

	<!-- 전체조회 -->
	<select id="getTodoList" reultType="ToDoVO">
		SELECT * FROM todo ORDER BY no	
	</select>
	
	<!-- 등록 -->]
	<insert id="insertTodo" parameterType="ToDoVO">
		INSERT INTO todo 
						 ( no, id, contents, todoyn)
		VALUES ( #{todoNo},
				 #{todoId},
				 #{contents},
				 #{todoyn}
					)
	</insert>
	
	<!-- 삭제 -->
	<delete id="deleteTodo" parameterType="Integer">
		DELETE FROM todo
			   WHERE id = #{todoId}
	</delete>
</mapper>